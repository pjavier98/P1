#include <stdio.h>

int main()
{
	int duas, primeira, apenas_uma, nenhuma;

	scanf("%d %d %d %d", &duas, &primeira, &apenas_uma, &nenhuma);

	printf("%d\n", duas + apenas_uma + nenhuma);

	return 0;
}